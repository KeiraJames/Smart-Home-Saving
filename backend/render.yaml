services:
  - type: web
    name: smart-homes-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FIREBASE_KEY_PATH
        value: firebase_key.json
